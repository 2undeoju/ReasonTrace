# Copy this file to .env and fill in your key to enable LLM-scored feedback.
# Without it, ReasonTrace runs in heuristic fallback mode automatically --
# no key required to try the API.
OPENAI_API_KEY=
